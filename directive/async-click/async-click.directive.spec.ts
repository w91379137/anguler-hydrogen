import { AsyncClickDirective } from './async-click.directive';

describe('AsyncClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncClickDirective();
    expect(directive).toBeTruthy();
  });
});
